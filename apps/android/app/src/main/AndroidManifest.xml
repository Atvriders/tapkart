<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            No android:screenOrientation. The lock was already unenforceable on
            the devices it mattered for — targetSdk 36 means Android 16 ignores
            it on any display with sw >= 600dp — so a tablet or an unfolded
            foldable could already boot portrait, and every viewport is now a
            real playable layout (R51).

            configChanges is what turns a fold, an unfold, a multi-window drag
            or a bar toggle into a resize instead of an activity recreation. A
            recreation reloads the WebView and drops the live race back to the
            title screen with no error anywhere, which gets debugged as a
            networking bug. density|fontScale are there because a foldable's
            inner and outer panels can report different densities, so an unfold
            mid-race is a density change as well as a size change.

            resizeableActivity is already the platform default for targetSdk
            >= 24. It is declared anyway so it is a stated intent that
            assert-manifest.mjs can actually see — an assertion cannot fail on
            an implicit default.
        -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|density|fontScale"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:theme="@style/AppTheme.NoActionBarLaunch">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links entry point. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="${tapkartHost}"
                    android:pathPrefix="${tapkartPathPrefix}"
                    android:scheme="https" />
            </intent-filter>

            <!-- Android 15 and earlier can dispatch the same NDEF URI directly. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:host="${tapkartHost}"
                    android:pathPrefix="${tapkartPathPrefix}"
                    android:scheme="https" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <service
            android:name=".nfc.TapkartHceService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>
    </application>
</manifest>
